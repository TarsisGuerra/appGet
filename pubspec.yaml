name: get_fluent
description: "A new Flutter project."
publish_to: 'none' # Remove this line if você deseja publicar para pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter_native_splash: ^2.4.0
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  firebase_core: ^2.3.0
  firebase_analytics: ^10.1.0
  firebase_auth: ^4.20.0
  cloud_firestore: ^4.17.5
  firebase_storage: ^11.7.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter_native_splash:    
  color: "#3DB2FF"
  image: assets/images/logoBranco.png

flutter:
  uses-material-design: true

  assets:
    - assets/images/logoBranco.png
